document.getElementById('bt-registro').addEventListener ('click', () => {
    setTimeout(() => window.location.href = 'registro.html', 600);
})
/*
document.getElementById('init-section').addEventListener ('click', () => {
   setTimeout(() => window.location.href = 'index.html', 600);
})  */
